# EzInfo Data Pipeline

EzInfo — это аддон‑справочник персонажей для WoW 3.3.5, который позволяет командой `/qq` мгновенно увидеть всех персонажей аккаунта: уровни, GS, расу, гильдию и т.д. Проект состоит из трёх логических частей:

1. **`RedSky.DoubleScout v0.5.3a.py`** — многопоточный парсер армори ezwow.org. Он авторизуется по cookies, выгружает списки персонажей, пишет результат в промежуточную БД и собирает финальный `ezbase_final_*.db`.
2. **`RedSky.DataInfuser v0.8a.py`** — генератор аддона. Берёт свежую `ezbase_final_*.db`, пересчитывает цвета, пересобирает `EzInfo.lua` и (опционально) копирует файл в папку аддонов WoW.
3. **`EzInfo/`** — сам аддон (TOC + Lua). После обновления базы /qq показывает нужную информацию прямо в игре, поддерживает автопоиск по текущей цели (`/qq on`).

## Содержимое репозитория

| Путь | Назначение |
| --- | --- |
| `EzInfo/` | Аддон для клиента WoW 3.3.5 (Lua + TOC). |
| `RedSky.DoubleScout v0.5.3a.py` | Парсер армори → SQLite. Требует cookies и интернет. |
| `RedSky.DataInfuser v0.8a.py` | Генератор Lua файла из финальной БД. |
| `cookies.md` | Шаблон/хранилище cookies для парсера. |
| `LOGS/`, `BASES/` (создаются скриптами) | Логи и базы, которые формируются во время работы. |

## Быстрый старт

1. **Установите Python ≥ 3.10** и зависимости:
   ```bash
   pip install -r requirements.txt
   ```
   или
   ```bash
   python -m pip install bs4 lxml requests tqdm
   ```

   
3. **Добавьте cookies** от ezwow.org в `cookies.md`. Скрипт ожидает формат `name=value; name2=value2; ...` во второй строке файла. Смотрите файл example_cookies.md. Он будет похож на тот который вы должны сделать самостоятельно но РАБОТАТЬ НЕ БУДЕТ, так как наполнен псевдопохожими значениями. Как получить кукис будет описанио в EXAMPLE_COOKIES.MD
4. **Запустите парсер** (создаст fresh базы):
   ```bash
   python "RedSky.DoubleScout v0.5.3a.py"
   ```
   - Укажите `PLAYTIME_ONLY`, задержки и т.п. в верхней части файла при необходимости.
   - Финальные базы появляются в `BASES/` (пример: `ezbase_final_YYMMDD_HHMM.db`), логи — в `LOGS/`.
5. **Сгенерируйте обновлённый аддон**:
   - В `RedSky.DataInfuser v0.8a.py` задайте `WoW_InterfaceFolderPath`, чтобы файл копировался сразу в `Interface\AddOns\EzInfo`.
   - Убедитесь, что нужная `ezbase_final_*.db` лежит в корне или в `BASES/`.
   - Выполните:
     ```bash
     python "RedSky.DataInfuser v0.8a.py"
     ```
   - Скрипт обновит `EzInfo/EzInfo.lua` (и при настроенном пути скопирует его в клиент).
6. **Скопируйте EzInfo.lua** (если не делаете этого автоматически) и EzInfo.toc (его достаточно скопировать один раз, он не изменяется скриптами): в папку с аддонами\EzInfo.

## Работа аддона в игре

- `/qq <имя_персонажа>` — показать всех персонажей аккаунта.
- `/qq <логин_аккаунта>` — то же, если знаете форумный аккаунт.
- `/qq` без аргументов — справка + статистика базы (источник, дата сборки, количество записей).
- `/qq on|off|status|memory` — управляет автопоиском по текущей цели и выводит служебные сведения.

## Важные детали

- **Сеть и авторизация.** Парсер работает только под авторизованным аккаунтом ezwow.org. При ошибках чтения страниц проверяйте валидность cookies.
- **Прерывание работы.** `Ctrl+C` корректно останавливает обе очереди скачивания и сохраняет прогресс.
- **Производительность.** Настройки `ENABLE_DELAYS`, `RANDOM_DELAY` и т.п. помогают уменьшить нагрузку на сайт, если требуется.
- **Локализация.** Lua код уже содержит русские строки и цветовые коды, поэтому используйте UTF‑8 при редактировании.

## Обновление базы шаг за шагом

1. Обновите cookies, если сессия устарела.
2. Запустите `RedSky.DoubleScout v0.5.3a.py` и дождитесь завершения потоков `playtime` и `name`.
3. Найдите свежий `ezbase_final_*.db` в `BASES/`.
4. Запустите `RedSky.DataInfuser v0.8a.py`, чтобы встроить эту базу в `EzInfo.lua`.
5. Перезапустите клиент WoW или перезагрузите UI (`/reload`) — новая база появится в игре.

## Требования

- Windows 10/11 (проверено, но скрипты кроссплатформенны).
- Python 3.10+.
- Библиотеки из `requirements.txt`.
- Доступ к ezwow.org (для парсера).

Готово! Теперь проект имеет полный цикл: от сбора данных до работающего аддона. При необходимости адаптируйте пути, настройки задержек. Удачной охоты на альтов, юный детектив! 🕵️‍♂️

